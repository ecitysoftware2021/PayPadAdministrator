@model IEnumerable<PayPlusModels.SP_GET_UPLOAD_PAYPAD_Result>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>GetUploads</title>
</head>
<body>
    <div id="portlet2" class="panel-collapse collapse in">
        <div class="portlet-body">
            <div class="table-responsive">
                <table class="table table-striped table-bordered display nowrap"
                       id="tableTransactions">
                    <thead>
                        <tr>
                            <th>
                                CÓDIGO USUARIO
                            </th>
                            <th>
                                VALOR CARGADO
                            </th>
                            <th>
                                VALOR PROGRAMADO
                            </th>
                            <th>
                                FECHA DE INICIO
                            </th>
                            <th>
                                FECHA FIN
                            </th>
                            <th>
                                DESCRIPCIÓN
                            </th>
                            <th>
                                ESTADO
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.USER_ID)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TOTAL_UPLOAD)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TOTAL_IN)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DATE_BEGIN)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DATE_END)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DESCRIPTION)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.STATE)
                                </td>
                                <td>
                                    <a href="@Url.Action("GetUploadsDetail", new {upload_detail_id = item.PAYPAD_UPLOAD_ID})"
                                       data-placement="top" data-toggle="tooltip" class="tooltips dialog-window" title="Mas detalle">
                                        <i class="fa fa-eye"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </thead>
                </table>
            </div>
        </div>
    </div>
</body>
</html>
@*<script type="text/javascript">
        var datetable ="";

        $(document).ready(function () {

            datetable = $('#tableTransactions').DataTable({
                dom: 'Bfrtip',
                buttons: [
                    'excel', 'pdf'
                ]
            });


        });
</script>*@